# The vendored fork resolves only from the public registry: a lockfile generated behind an
# internal mirror records that mirror's URLs and then fails to install anywhere else.
registry=https://registry.npmjs.org/
# Mirrors the repository policy: do not resolve packages published within the last five days.
min-release-age=5
