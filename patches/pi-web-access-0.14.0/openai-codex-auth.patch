--- a/openai-search.ts
+++ b/openai-search.ts
@@ -10,8 +10,10 @@
 const CONFIG_PATH = getWebSearchConfigPath();
 const SEARCH_TIMEOUT_MS = 60_000;
 
+// Local modification: the openai-codex candidate is removed so search never binds to the Codex
+// subscription JWT, whose quota is shared with interactive coding use. Auth resolution now falls
+// through to the configured openaiApiKey / OPENAI_API_KEY and the api.openai.com endpoint.
 const AUTH_MODEL_CANDIDATES = [
-	{ provider: "openai-codex", models: ["gpt-5.4", "gpt-5.3-codex", "gpt-5.3-codex-spark", "gpt-5.2", "gpt-5.2-codex"] },
 	{ provider: "openai", models: ["gpt-5.4", "gpt-5.2", "gpt-4.1-mini", "gpt-4o"] },
 ] as const;
 
